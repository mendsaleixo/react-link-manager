generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Link {
  id        String   @id @default(uuid())
  title     String
  url       String
  createdAt DateTime @default(now())
}